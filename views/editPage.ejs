<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta-name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>

    <style>
        /* Navbar */
        .navbar {
            background-color: #2c3e50;
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
            font-family: Arial;
        }

        .navbar a {
            color: white;
            margin: 0 10px;
            text-decoration: none;
            font-weight: bold;
        }

        body {
            margin: 0;
            background: #ecf0f1;
            font-family: Arial, Helvetica, sans-serif;
        }

        /* Card */
        .card {
            width: 450px;
            margin: 60px auto;
            background: white;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0px 4px 15px rgba(0,0,0,0.1);
        }

        .card-title {
            font-size: 26px;
            font-weight: bold;
            color: #2c3e50;
        }

        .card-subtitle {
            font-size: 16px;
            color: #7f8c8d;
            margin-top: 5px;
        }

        /* Form */
        form {
            margin-top: 20px;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
            display: block;
            color: #34495e;
        }

        input, textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
            margin-bottom: 15px;
            font-size: 14px;
        }

        textarea {
            height: 100px;
            resize: none;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #2c3e50;
            color: white;
            border: none;
            font-size: 16px;
            border-radius: 6px;
            cursor: pointer;
        }

        button:hover {
            background-color: #1a242f;
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <div class="navbar">
        <div class="logo">MyWebsite</div>
        <div class="links">
            <a href="#">Home</a>
            <a href="#">Login</a>
            <a href="#">Help</a>
        </div>
    </div>

    <!-- Card With Form -->
    <div class="card">
        <div class="card-title">Registration Form</div>
        <div class="card-subtitle">Fill the details below</div>

        <!-- Added action + method -->
        <form action="/editBlog/<%-data[0].id%>" method="POST">

            <label for="title">Title</label>
            <input type="text" id="title" name="title" value="<%- data[0].title %>"  placeholder="Enter title" >

            <label for="subTitle">Subtitle</label>
            <input type="text" id="subTitle" name="subTitle" value="<%- data[0].subTitle%>" placeholder="Enter subtitle">

            <label for="description">Description</label>
            <textarea id="description" name="description"  placeholder="Enter description">
                <%-data[0].description%>
            </textarea>

            <button type="submit">Submit</button>
        </form>
    </div>

</body>
</html>
